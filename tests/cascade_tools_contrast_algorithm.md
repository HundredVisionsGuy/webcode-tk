# Color Contrast Report Algorithm

```
SET report_data to empty list
GET a_rating
SET global_colors to css_tools.get_project_global_colors()
FOREACH file in global_colors
	IF fails normal a_rating
		APPEND f"{filename} selector, {selector} fails normal text at {a_rating} rating."

FOREACH file in project folder
    GET cascade_tree
    IF
    GET cascade tree
    FOREACH element in the tree
    	IF fails Normal
    		GET if set directly
```
